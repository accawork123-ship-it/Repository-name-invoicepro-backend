name: newapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.6   # ✅ FIXED (correct place)
  intl_phone_number_input: ^0.7.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1
  

flutter:
  uses-material-design: true

  assets:
    - assets/InvoicePro.png

# ✅ App Icon Configuration
flutter_icons:
  android: true
  ios: true
  image_path: "assets/InvoicePro.png"
